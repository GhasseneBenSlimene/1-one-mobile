<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.one_mobile">

    <!-- Permission required to check the network connection state -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Permission required to access the internet -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/one_mobile"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        
        <activity android:name="com.example.one_mobile.ui.auth.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.Home" />
        <activity android:name=".ui.evaluationUtr.EvaluationListByUtr" />
        <activity android:name=".ui.evaluationPost.EvaluationListByPost" />
        <activity android:name=".ui.evaluationAction.EvaluationListByAction" />
        <activity android:name=".ui.evaluationSite.EvaluationListBySite" />
        <activity android:name=".ui.evaluationUtr.ActionPlansByUtr" />
        <activity android:name=".ui.evaluationUtr.EvaluationUtrForm" />
        <activity android:name=".ui.evaluationSite.EvaluationSiteForm" />
        <activity android:name=".ui.evaluationSite.EvaluationSiteUpdateForm" />



    </application>


</manifest>